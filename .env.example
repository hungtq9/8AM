# GreenNode MaaS — OpenAI-compatible endpoint
LLM_ENDPOINT=https://maas.greennode.ai/v1
LLM_API_KEY=your_maas_api_key_here
LLM_MODEL=Qwen-3-27B

# AgentBase credentials (for deploy only, not needed at runtime)
# GREENNODE_CLIENT_ID=your_client_id
# GREENNODE_CLIENT_SECRET=your_client_secret
